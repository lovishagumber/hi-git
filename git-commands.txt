git init - it initialise a repository
git config --global user.name"lovishagumber" - tell who you are
git config --global user.email"gumberlovisha7@gmail.com" - tell who you are
git add . - add files
git commit -m - commit changes to head
git push origin master - send changes to the master branch of remote repository
git status - list the file you have changed and those you need to add or commit
